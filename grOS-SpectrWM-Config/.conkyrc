out_to_x no
out_to_console yes
update_interval 1.0
total_run_times 0
use_spacer none
alignment top_center
TEXT
KERNEL:$kernel // DATE:${time %d|%m|%y} // TIME:${time %H:%M:%S}  // UPTIME:${uptime_short} // RAM:$memperc% // CPU:${cpu}% // ROOT:${fs_used_perc /}% // HOME:${fs_used_perc /home}% // SENSORS:42° // HDDTEMP:40° // IP:${addr wlan0} // DOWNLOADS:${downspeedf wlan0} // UPLOADS:${upspeedf wlan0}
